services:
  db:
    image: postgres:15-alpine
    container_name: study-summarizer-db
    restart: always
    env_file:
      - ./backend/.env
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
